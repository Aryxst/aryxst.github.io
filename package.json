{
 "name": "aryxst",
 "scripts": {
  "build": "tailwindcss  -i src/style.css -o src/style.min.css --minify",
  "build:css": "tailwindcss  -i src/style.css -o src/style.min.css --watch --minify",
  "expose": "ssh -R jessieodeh:80:localhost:3000 serveo.net"
 },
 "dependencies": {
  "tailwindcss": "^3.4.1"
 },
 "devDependencies": {
  "@types/bun": "latest",
  "autoprefixer": "^10.4.19",
  "prettier-plugin-tailwindcss": "^0.5.12"
 }
}
